extends ../layout/index

block contenido 
    div.py-10
        h1.text-6xl.my-10.font-extrabold.text-center Inmo
            span.font-normal Linking             
        h2.text-2xl.text-center.font-extrabold= pagina /* Email confirm */

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-log.w-full.font-bold.text-center.text-white.uppercase= message

        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya Puedes Iniciar Sesión